apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flowresta.fullname" . }}-test-connection"
  labels:
    {{- include "flowresta.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flowresta.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
