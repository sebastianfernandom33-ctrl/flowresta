apiVersion: v1
kind: Service
metadata:
  name: {{ include "flowresta.fullname" . }}
  labels:
    {{- include "flowresta.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flowresta.selectorLabels" . | nindent 4 }}
